<%= stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" %>

<h3 class="mb-4">
  <%= @business.name %>
  <small class="text-muted">(<%= format_month_date(Date.today) %>)</small>
</h3>

<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      <th scope="col">Phone</th>
    </tr>
  </thead>
  <tbody>
    <% @clients.each do |client| %>
      <tr class="text-center">
        <th scope="row">
          <%= client.id %>
        </th>
        <td>
          <%= client.name %>
        </td>
        <td>
          <% unless client.email.empty? %>
            <%= client.email %>
          <% else %>
            No email provided
          <% end %>
        </td>
        <td>
          <% unless client.phone.empty? %>
            <%= client.phone %>
          <% else %>
            No phone provided
          <% end %>
          <%= client.phone %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
