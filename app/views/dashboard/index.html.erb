<div class="h-full bg-gray-100 font-sans">
  <%= render 'sidebar' %>
  <%= render 'dashboard/partials/navbar' %>
  <div
    class="
      dashboard-content
      flex
      flex-row
      flex-wrap
      flex-1
      flex-grow
      content-start
      pl-16
    "
  >
    <!--Dash Content -->
    <%= render 'dashboard/partials/dash_content' %>
    <!--Graph Content -->
    <div class="flex flex-1">
      <%= line_chart(@data, width: "99%", height: "100%") %>
    </div>
  </div>
</div>
